version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    ports:
      - "3000:443"
    env_file:
      - ./insighthub-backend/.env.prod
    volumes:
      - ./ssl:/ssl
